import { CodeGenerator } from "./default-code-generator";
export declare function generateValidator({ cwd, validatorGenerator, tsconfigFilePath, filePath, extraTags }: {
    cwd: string;
    validatorGenerator: CodeGenerator;
    tsconfigFilePath: string;
    filePath: string;
    extraTags?: string[];
}): Promise<{
    validatorFilePath: string;
    code: string;
} | undefined>;
