export declare function verify_bytes(public_key: string, bytes: string, signature: string): Promise<boolean>;
export declare function verify(address: string, edpk: string, message: string, signature: string): Promise<boolean>;
