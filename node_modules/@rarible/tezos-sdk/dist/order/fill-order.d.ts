import { MichelsonData } from "@taquito/michel-codec";
import { Provider, OperationResult } from "@rarible/tezos-common";
import { Part, OrderForm } from "./utils";
import BigNumber from "bignumber.js";
export interface FillOrderRequest {
    amount: BigNumber;
    payouts?: Array<Part>;
    origin_fees?: Array<Part>;
    infinite?: boolean;
    use_all?: boolean;
    edpk?: string;
}
export declare function match_order_to_struct(p: Provider, left: OrderForm, right: OrderForm): Promise<MichelsonData>;
export declare function fill_order(provider: Provider, left: OrderForm, request: FillOrderRequest, unwrap?: boolean, unwrap_amount?: BigNumber): Promise<OperationResult>;
