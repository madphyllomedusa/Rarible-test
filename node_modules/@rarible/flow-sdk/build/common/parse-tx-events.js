"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseEvents = void 0;
function parseEvents(events, eventName, field) {
    var event = events.find(function (e) {
        var _a = e.type.split("."), event = _a[3];
        return event === eventName;
    });
    if (!event) {
        throw new Error("Event ".concat(eventName, " not found in transaction events"));
    }
    if (!(field in event.data)) {
        throw new Error("Field ".concat(field, " not found in event data"));
    }
    return event.data[field];
}
exports.parseEvents = parseEvents;
