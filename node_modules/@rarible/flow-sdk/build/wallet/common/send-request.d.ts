import type { FlowAddress } from "@rarible/types";
import type { FlowCurrency, FlowNetwork } from "../../types";
export declare function sendRequest(network: FlowNetwork, currency: FlowCurrency, address: FlowAddress): Promise<any>;
