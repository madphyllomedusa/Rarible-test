import type { Fcl, FclArgs } from "@rarible/fcl-types";
import type { FlowCurrency } from "../types";
declare type GetBalanceCode = {
    cadence: string;
    args: ReturnType<FclArgs>;
};
export declare function getBalanceCode(fcl: Fcl, currency: FlowCurrency, address: string): GetBalanceCode;
export {};
