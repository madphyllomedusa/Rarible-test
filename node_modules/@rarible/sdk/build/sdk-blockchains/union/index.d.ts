import type { IRaribleInternalSdk } from "../../domain";
export declare function createUnionSdk(ethereum: IRaribleInternalSdk, flow: IRaribleInternalSdk, tezos: IRaribleInternalSdk, polygon: IRaribleInternalSdk, solana: IRaribleInternalSdk): IRaribleInternalSdk;
