import { BigNumber } from "@rarible/types";
import { FlowAddress } from "@rarible/types";
import { FlowCreator } from "./FlowCreator";
import { FlowRoyalty } from "./FlowRoyalty";
import { Meta } from "./Meta";
export declare type FlowNftItem = {
    id: string;
    collection: string;
    tokenId: BigNumber;
    creators: Array<FlowCreator>;
    owner?: FlowAddress;
    royalties: Array<FlowRoyalty>;
    metaUrl?: string;
    meta?: Meta;
    mintedAt: string;
    lastUpdatedAt: string;
    supply: BigNumber;
    deleted: boolean;
};
