import { BigNumber } from "@rarible/types";
import { FlowAddress } from "@rarible/types";
import { PayInfo } from "./PayInfo";
export declare type FlowNftOwnership = {
    id?: string;
    contract: string;
    tokenId: BigNumber;
    owner: FlowAddress;
    creators: Array<PayInfo>;
    createdAt: string;
};
