import { Address } from "@rarible/types";
import { BigNumber } from "@rarible/types";
export declare type OrderCollectionStats = {
    token: Address;
    floorPrice?: BigNumber;
    totalVolume?: BigNumber;
    highestSale?: BigNumber;
    lastUpdatedAt?: string;
};
