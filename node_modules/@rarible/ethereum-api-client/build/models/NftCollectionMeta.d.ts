import { Address } from "@rarible/types";
import { NftMedia } from "./NftMedia";
export declare type NftCollectionMeta = {
    name: string;
    description?: string;
    image?: NftMedia;
    external_link?: string;
    seller_fee_basis_points?: number;
    fee_recipient?: Address;
};
