import { Address } from "@rarible/types";
import { BigNumber } from "@rarible/types";
export declare type NftCollectionStats = {
    token: Address;
    totalItemSupply?: BigNumber;
    totalOwnerCount?: number;
    lastUpdatedAt?: string;
};
