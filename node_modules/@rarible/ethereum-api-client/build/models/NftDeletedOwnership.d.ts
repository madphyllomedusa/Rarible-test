import { Address } from "@rarible/types";
import { BigNumber } from "@rarible/types";
export declare type NftDeletedOwnership = {
    id: string;
    token: Address;
    tokenId: BigNumber;
    owner: Address;
};
