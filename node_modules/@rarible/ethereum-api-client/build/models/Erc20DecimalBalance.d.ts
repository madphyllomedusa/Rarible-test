import { Address } from "@rarible/types";
import { BigNumber } from "@rarible/types";
export declare type Erc20DecimalBalance = {
    contract: Address;
    owner: Address;
    balance: BigNumber;
    decimalBalance: BigNumber;
    createdAt?: string;
    lastUpdatedAt?: string;
};
