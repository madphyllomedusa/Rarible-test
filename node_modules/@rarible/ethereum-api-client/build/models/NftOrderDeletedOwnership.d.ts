import { Address } from "@rarible/types";
import { BigNumber } from "@rarible/types";
export declare type NftOrderDeletedOwnership = {
    id: string;
    token: Address;
    tokenId: BigNumber;
    owner: Address;
};
