import type { NftCollection } from "@rarible/ethereum-api-client";
export declare type PrepareMintResponse = {
    multiple: boolean;
    supportsRoyalties: boolean;
    supportsLazyMint: boolean;
};
export declare function isSupportsRoyalties(collection: NftCollection): boolean;
export declare function isSupportsLazyMint(collection: NftCollection): boolean;
export declare function isMultiple(collection: NftCollection): boolean;
export declare function prepareMintRequest(collection: NftCollection): PrepareMintResponse;
