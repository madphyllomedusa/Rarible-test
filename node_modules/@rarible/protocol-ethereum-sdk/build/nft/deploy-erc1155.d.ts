import type { Ethereum, EthereumTransaction } from "@rarible/ethereum-provider";
import type { Address } from "@rarible/types";
import type { Maybe } from "@rarible/types/build/maybe";
import type { SendFunction } from "../common/send-transaction";
import type { EthereumConfig } from "../config/type";
export declare class DeployErc1155 {
    private readonly ethereum;
    private readonly send;
    private readonly config;
    constructor(ethereum: Maybe<Ethereum>, send: SendFunction, config: EthereumConfig);
    deployToken(name: string, symbol: string, baseURI: string, contractURI: string): Promise<{
        tx: EthereumTransaction;
        address: Address;
    }>;
    private getContractAddress;
    deployUserToken(name: string, symbol: string, baseURI: string, contractURI: string, operators: Address[]): Promise<{
        tx: EthereumTransaction;
        address: Address;
    }>;
    private getUserContractAddress;
}
