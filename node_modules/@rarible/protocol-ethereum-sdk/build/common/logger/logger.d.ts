import { RemoteLogger } from "@rarible/logger/build";
import type { Maybe } from "@rarible/types/build/maybe";
import type { Ethereum } from "@rarible/ethereum-provider";
import type { EthereumNetwork } from "../../types";
declare type Environment = "prod" | "e2e" | "dev" | "staging";
/**
 * Convert network name to stage environment name
 *
 * @param network
 */
export declare function getEnvironment(network: EthereumNetwork): Environment;
export declare function getErrorMessageString(err: any): string;
export declare function createRemoteLogger(context: {
    ethereum: Maybe<Ethereum>;
    env: Environment;
}): RemoteLogger;
export {};
