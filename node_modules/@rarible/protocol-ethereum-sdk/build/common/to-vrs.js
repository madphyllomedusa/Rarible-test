"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toVrs = void 0;
var types_1 = require("@rarible/types");
function toVrs(sig) {
    var sig0 = sig.startsWith("0x") ? sig.substring(2) : sig;
    var r = "0x" + (sig0.substring(0, 64) || types_1.ZERO_WORD.substring(2));
    var s = "0x" + (sig0.substring(64, 128) || types_1.ZERO_WORD.substring(2));
    var v = parseInt(sig0.substring(128, 130), 16) || 0;
    return { r: r, v: v < 27 ? v + 27 : v, s: s };
}
exports.toVrs = toVrs;
