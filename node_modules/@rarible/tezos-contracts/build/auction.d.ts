import { Provider, DeployResult } from "@rarible/tezos-common";
import BigNumber from "bignumber.js";
export declare const auction_code: any;
export declare function auction_storage(owner: string, default_fee_receiver: string, protocol_fee: BigNumber, royalties_provider: string): any;
export declare function deploy_auction(provider: Provider, owner: string, default_fee_receiver: string, protocol_fee: BigNumber, royalties_provider: string): Promise<DeployResult>;
