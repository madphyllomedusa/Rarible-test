export declare class Batcher<T> {
    private readonly interval;
    private readonly handler;
    private timeout?;
    private queue;
    constructor(interval: number, handler: (queue: T[]) => void);
    add(item: T): void;
    drop(): void;
}
